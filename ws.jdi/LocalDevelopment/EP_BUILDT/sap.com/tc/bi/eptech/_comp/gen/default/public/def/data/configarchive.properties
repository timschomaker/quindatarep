# Version info file
config.version.major        = 6
config.version.minor        = 0
config.version.component    = 1
config.latest.change        = 0

# value for configarchive.manifest.token.application-version
config.archive-version        = ${config.version.major}.${config.version.minor}.${config.version.component}.${config.latest.change}
configmeta.archive-version    = ${config.version.major}.${config.version.minor}.${config.version.component}.${config.latest.change}
